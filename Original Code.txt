#include <iostream>
#include <array>
#include <fstream>

using namespace std;

void handleLogin(){
    string username, password, un, pw;
    bool loggedIn = false;
    
    //If statement to see what the user inputs
    cout << "Please Enter Your Username: ";
    cin >> username;
    cout << "Please Enter Your Password: ";
    cin >> password;
    
    //fstream creates and reads the file
    ifstream read(username + ".txt");
    

    //Checks if the Username is valid
    if (read){
        getline(read, un);
        getline(read, pw);
        if (un == username && pw == password){
            loggedIn = true;
            cout << "Success! You're logged in!" << endl;
        }
        else{
            cout << "Your Username or Password is incorrect! Please check your spelling!" << endl;
        }
    read.close()
    }  
    //End of Login code --------------------------------------------------------------------
    //Registration code
void handleRegistration(){
    {
        cout << "Please Enter the Username You Want to Use: ";
        cin >> username;
        cout << "Please Enter Your Password: ";
        cin >> password;
        ofstream file;
        file.open(username + ".txt");
        file << username << endl << password;
        file.close();
        cout << "Successfully Created Your Account, Please Login!" << endl;
    }
    while (loggedIn == true){
        
    }
}
void handleMainMenu(){
    //Beginning of program
    string loginOrRegister;

    cout << "\t\t\t_________________________________________________\n\n\n";
    cout << "\t\t\t     Welcome to The Student Management System!    \n\n\n";
    //Asks the user if they want to either Login or Register
    cout << "Login or Register: ";
    cin >> loginOrRegister;

    if (loginOrRegister == "Login" || loginOrRegister == "LOGIN" || loginOrRegister == "login"){
        handleLogin()
    }
    if (loginOrRegister == "Register" || loginOrRegister == "REGISTER" || loginOrRegister == "register"){
        handleRegistration();
    }
    else{
        cout << "Invalid Choice: Please Enter "Login" or "Register" as Your Choices!" << endl;
        displayMainMenu();
    }
}


int main() {
    handleMainMenu();
    return 0;
}